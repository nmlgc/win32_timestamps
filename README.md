Operations on all Win32 timestamps, including the ones that are not typically supported by Unix-native tools.
Currently supports:

* Dumping timestamps of a directory tree to a text format and writing them back
